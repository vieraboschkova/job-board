export enum ApiMountPath {
  Api = "/api",
}

export enum ApiRoutePath {
  Health = "/health",
  Ingest = "/ingest",
  Jobs = "/jobs",
  JobsSearch = "/jobs/search",
  JobById = "/jobs/:id",
  Docs = "/docs",
  DocsJson = "/docs.json",
}
